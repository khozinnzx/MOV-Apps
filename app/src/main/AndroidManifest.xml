<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mov_apps">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MOVApps">
        <activity android:name=".ui.MainActivity"></activity>
        <activity android:name=".ui.SignUpPhotoActivity" />
        <activity android:name=".ui.SignUpActivity" />
        <activity android:name=".ui.SignInActivity" />
        <activity android:name="com.example.mov_apps.ui.onboarding.OnBoardingThreeActivity" />
        <activity android:name="com.example.mov_apps.ui.onboarding.OnBoardingTwoActivity" />
        <activity android:name="com.example.mov_apps.ui.onboarding.OnBoardingOneActivity" />
        <activity android:name=".ui.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>